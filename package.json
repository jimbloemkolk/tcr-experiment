{
  "name": "tcr-experiment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
     "test": "jest",
     "tcr": "npm run test && (git add . && git commit --amend --no-edit) || (git add . && git reset --hard)"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.4",
    "ts-jest": "^27.0.5",
    "typescript": "^4.4.3"
  }
}
